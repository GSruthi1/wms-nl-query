[tool.ruff]
line-length = 100
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B"]
# E501: line length, not worth the churn. B008: FastAPI's Depends()-in-
# argument-defaults is the framework's required idiom, not a bug.
ignore = ["E501", "B008"]
