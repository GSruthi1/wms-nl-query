[pytest]
testpaths = tests
python_files = test_*.py
addopts = -v --strict-markers
asyncio_mode = strict
asyncio_default_fixture_loop_scope = function
markers =
    benchmark: paid LLM benchmark eval — not run in default CI, only via -m benchmark or workflow_dispatch
