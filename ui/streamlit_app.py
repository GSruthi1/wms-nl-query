"""Placeholder — built out in the UI phase (see build plan). Scaffold-only for now."""
import streamlit as st

st.title("WMS Natural-Language Query Interface")
st.write("UI under construction — coming after the NL→SQL pipeline and eval framework.")
