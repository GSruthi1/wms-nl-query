fastapi==0.115.0
uvicorn[standard]==0.30.6
sqlalchemy==2.0.35
alembic==1.13.2
psycopg2-binary==2.9.9
pydantic==2.9.2
pydantic-settings==2.5.2
python-multipart==0.0.9
anthropic==0.34.2
openai==1.47.1
# Pinned explicitly, not left to transitive resolution: anthropic==0.34.2's
# internal httpx client wrapper unconditionally passes a `proxies=` kwarg
# that httpx>=0.28 removed, so an unpinned httpx breaks LLM calls with
# `TypeError: Client.__init__() got an unexpected keyword argument 'proxies'`.
# This is a real, previously-hit production bug (see LDIP's history), not
# hypothetical — pinning it here up front instead of rediscovering it.
httpx==0.27.2
sqlglot==25.24.5
python-dotenv==1.0.1
# Capped at 8.x, not 9.x: streamlit==1.38.0 (requirements-dev.txt) requires
# tenacity<9, so pinning 9.x here would make the dev/CI install unresolvable.
tenacity==8.5.0
structlog==24.4.0
pyyaml==6.0.2
faker==29.0.0
pandas==2.2.3
